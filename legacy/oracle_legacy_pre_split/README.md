# Legacy

This directory acts as an archive for deprecated scripts, older versions of the pipeline, and experimental patches.

These files are preserved for historical reference and to track the evolution of the plantclef architecture. Code here is not actively maintained and should not be imported into the current `phases/` or `src/` modules.