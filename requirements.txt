# Runtime requirements for the i002 paper pipeline.
#
# These are the only dependencies needed to train, run inference, and
# reproduce the headline submission of report/main.tex. The legacy
# pipeline in legacy/ used a much larger stack (NVIDIA DALI, DeepSpeed,
# cuDF, PEFT, SAM, etc.); none of that is required for the paper code.

# --- Core deep-learning stack -----------------------------------------------
torch>=2.2.0
torchvision>=0.17.0

# --- Foundation model: BioCLIP 2.5 ViT-H/14 ---------------------------------
open_clip_torch>=2.24.0

# --- Data + image I/O -------------------------------------------------------
pandas>=2.1.0
numpy>=1.26.0
Pillow>=10.0.0

# --- Training-time utilities ------------------------------------------------
tqdm
python-dotenv

# --- Optional: experiment tracking ------------------------------------------
# Enabled by passing --wandb to src/train.py; omit if you don't need it.
wandb>=0.16.0
